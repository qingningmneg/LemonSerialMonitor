# Lemon串口监控 0.1.0 发布验收记录

这份记录只写实际执行过的结果。自动化测试、源码事务实测和最终图形安装包实测分开记录，不能相互替代。

最终发布文件以 GitHub `v0.1.0` Release 为准：源码提交见同版本 `BUILD-INFO.json`，安装包精确 SHA-256 见同版本 `SHA256SUMS.txt`。本记录不把安装包哈希写进安装包载荷，避免安装包校验自身的循环依赖。

## 验收环境

- 系统：Windows 11 专业版 x64，版本 10.0.26100
- 电脑：LENOVO 21D1
- 验收日期：2026-07-15（Asia/Shanghai）
- 当前可用 Windows Server 实机/虚拟机：无
- 当前枚举到的物理串口：无；真实串口捕获已在前一轮本机联调中验证，最终包仍需在接入串口设备后补做持续捕获

## 自动化门禁

- [x] 托管测试：592/592 通过
- [x] PowerShell 安装安全测试：900/900 通过
- [x] 原生 C/C++ 测试：5/5 通过，全部使用 `/W4 /WX`
- [x] 内核协议编译门禁通过
- [x] WDK Release 驱动构建与代码分析：0 条诊断
- [x] INF 验证：退出代码 0
- [x] 公开名称审计通过
- [x] `git diff --check` 通过
- [x] 发布包严格限制为安装程序、PDF 手册、发布说明、构建信息和 SHA-256 清单五个文件
- [x] DOCX 结构检查通过；PDF 共 14 页，已逐页检查无缺字、重叠、断表和乱码

## 源码安装事务实测

- [x] 受保护的旧手工安装通过身份、路径、哈希和权限核验后完成迁移
- [x] 驱动服务与后台服务均进入 Running
- [x] 完整卸载只留下 Windows 正在占用的内核服务，状态准确返回 `PendingReboot`
- [x] 重启续办任务以 SYSTEM 身份完成剩余清理并自行删除
- [x] 重启后驱动服务、后台服务、程序目录、数据目录、AI 状态、测试证书和本软件过滤器条目均不存在
- [x] 卸载没有删除未归本次安装所有的外部对象

## 最终图形安装包

- [x] 中文图形向导可双击启动
- [x] 欢迎页、证书协议、安装位置、快捷方式和准备安装页可正常进入
- [x] 桌面程序安装位置可选择，默认值为 `%ProgramFiles%\Lemon串口监控`
- [x] 测试证书风险、TESTSIGNING、安全启动和完整数据删除说明已显示
- [ ] 最终候选通过向导完成安装
- [ ] 驱动服务与后台服务启动
- [ ] `status --json` 与 `ports --json` 返回有效 JSON
- [ ] MCP 初始化、ping、工具清单和资源清单完成实机冒烟
- [ ] 本机没有新增 HTTP/TCP 监听
- [ ] 桌面程序可启动并连接后台服务
- [ ] Windows 设置中的卸载入口可用
- [ ] 最终候选完成卸载；如需要重启，重启续办后残留为零
- [ ] 卸载完成后重新安装最终发布包，给本机保留可直接使用的状态

## 串口功能

- [x] 桌面程序自身不直接打开被监控 COM 端口
- [x] 前一轮真实联调已看到串口读取事件
- [x] 多端口、暂停、继续、停止、确认清空、列表、Dump、终端、查找、复制和导出均有自动化测试
- [ ] 当前电脑重新接入串口设备后，用最终发布包补做 Read、Write、Ioctl 和长时间捕获

## Windows Server

- [x] Server 2019 桌面体验与 Server Core 平台矩阵通过
- [x] Server 2022 桌面体验与 Server Core 平台矩阵通过
- [x] Server 2025 桌面体验与 Server Core 平台矩阵通过
- [x] Server Core 组件布局不包含 WPF 和快捷方式，只包含驱动、服务、AI 接口、状态工具和文档
- [x] 发布说明明确写明本次没有 Server 实机/虚拟机验收，不把自动化兼容矩阵写成真实硬件认证
- [ ] 后续在至少一台实际 Windows Server 上完成安装、捕获、AI 和完整卸载

## 当前结论

- 状态：发布前最终图形安装包实测进行中
- 已知未完成项：当前无物理串口与 Windows Server 测试机
- 发布条件：最终候选安装、AI 冒烟、卸载/重装和五文件独立核验全部通过
