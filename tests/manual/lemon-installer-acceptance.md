# Lemon串口监控 0.1.0 发布验收记录

这份记录只写实际执行过的结果。自动化测试、源码事务实测和最终图形安装包实测分开记录，不能相互替代。

最终发布文件以 GitHub `v0.1.0` Release 为准：源码提交见同版本 `BUILD-INFO.json`，安装包精确 SHA-256 见同版本 `SHA256SUMS.txt`。本记录不把安装包哈希写进安装包载荷，避免安装包校验自身的循环依赖。

## 验收环境

- 系统：Windows 11 专业版 x64，版本 10.0.26100
- 电脑：LENOVO 21D1
- 验收日期：2026-07-15（Asia/Shanghai）
- 当前可用 Windows Server 实机/虚拟机：无
- 当前枚举到的物理串口：无；真实串口捕获已在前一轮同机联调中验证，当前候选重点完成无设备冷启动与接口验收

## 自动化门禁

- [x] 托管测试：615/615 通过（Core 120、Service 293、App 66、AI 18、UninstallHelper 118）
- [x] PowerShell 安装安全测试：919/919 通过
- [x] 原生 C/C++ 测试：5/5 通过，全部使用 `/W4 /WX`
- [x] 内核协议编译门禁通过
- [x] WDK Release 驱动构建与代码分析：0 条诊断
- [x] INF 验证：退出代码 0
- [x] 公开名称审计通过
- [x] `git diff --check` 通过
- [x] 发布包严格限制为安装程序、PDF 手册、发布说明、构建信息和 SHA-256 清单五个文件
- [x] DOCX 结构与元数据检查通过；PDF 共 15 页，已逐页检查无缺字、重叠、断表和乱码

## 源码安装事务实测

- [x] 受保护的旧手工安装通过身份、路径、哈希和权限核验后完成迁移
- [x] 驱动服务与后台服务均进入 Running
- [x] 完整卸载只留下 Windows 正在占用的内核服务，状态准确返回 `PendingReboot`
- [x] 重启续办任务以 SYSTEM 身份完成剩余清理并自行删除
- [x] 重启后驱动服务、后台服务、程序目录、数据目录、AI 状态、测试证书和本软件过滤器条目均不存在
- [x] 卸载没有删除未归本次安装所有的外部对象

## 最终图形安装包

- [x] 中文图形向导可双击启动
- [x] 欢迎页、证书协议、安装位置、快捷方式和准备安装页可正常进入
- [x] 桌面程序安装位置可选择，默认值为 `%ProgramFiles%\Lemon串口监控`
- [x] 测试证书风险、TESTSIGNING、安全启动和完整数据删除说明已显示
- [x] 当前代码候选通过向导完成干净安装并按提示重启
- [x] 无串口设备时后台服务重启后为 `Running`、启动类型为 `Automatic`；按需过滤驱动未绑定设备而保持停止是预期状态
- [x] 本次启动后没有本软件后台服务对应的 SCM 失败事件或 .NET Runtime 崩溃事件
- [x] `status --json`、`ports --json`、`schema --json` 与 `sessions list --json` 均返回有效结果；无设备时端口数组为空
- [x] MCP 以协议 `2025-03-26` 完成初始化，列出 11 个工具和 4 个资源，并实际调用状态与端口工具
- [x] 后台服务没有新增 HTTP/TCP 监听
- [x] 桌面程序可启动；点击“刷新端口”后显示服务已连接、驱动暂不可用、事件 0、丢失 0
- [x] Windows 设置中的卸载入口可用
- [x] 前一候选完成完整卸载且五个产品根目录、服务、驱动和卸载入口清理完成；随后完成当前代码候选的干净安装
- [ ] 公开资料更新后，用最终五文件 Release 中的精确安装包再做一次卸载、干净安装、重启和接口冒烟，并给本机保留可直接使用状态

## 串口功能

- [x] 桌面程序自身不直接打开被监控 COM 端口
- [x] 前一轮同机真实联调会话记录 3653 个事件，界面与服务丢失计数均为 0
- [x] AI 分页读取覆盖全部 3653 个事件，完整性结果为无驱动丢弃、无服务丢弃、无序号缺口且返回范围完整
- [x] 多端口、暂停、继续、停止、确认清空、列表、Dump、终端、查找、复制和导出均有自动化测试
- [x] 生产组合自动化覆盖“服务先在无设备状态启动、之后设备可用时恢复探测”路径
- [ ] 当前电脑重新接入串口设备后，用最终发布包补做热插拔、Read、Write、Ioctl 和长时间捕获

## Windows Server

- [ ] Server 2019 自托管任务未执行
- [x] Server 2022 GitHub 托管桌面 runner 的平台识别、托管测试和安装契约检查通过；未装载内核驱动
- [x] Server 2025 GitHub 托管桌面 runner 的平台识别、托管测试和安装契约检查通过；未装载内核驱动
- [x] Server Core 组件布局契约不包含 WPF 和快捷方式，只包含驱动、服务、AI 接口、状态工具和文档；未在真实 Server Core 中安装
- [x] 发布说明明确写明本次没有 Server 实机/虚拟机驱动端到端验收，不把契约检查写成真实硬件认证
- [ ] 后续在至少一台实际 Windows Server 上完成驱动安装、重启、捕获、AI 和完整卸载

## 当前结论

- 状态：代码候选、无设备冷启动、桌面、CLI、MCP、卸载与干净重装已通过；公开资料和最终五文件 Release 正在生成
- 已知未完成项：当前无物理串口与 Windows Server 测试机，最终精确发布包尚待重建和回下载核验
- 发布条件：手册逐页检查、最终候选完整重建、精确安装包卸载/重装、五文件独立核验和 GitHub 回下载校验全部通过
